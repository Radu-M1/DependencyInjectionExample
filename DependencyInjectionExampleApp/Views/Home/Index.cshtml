@model List<string>
@{
    ViewData["Title"] = "Home";
}

<h1>@ViewData["Title"]</h1>
<p>Welcome to the Home page!</p>

<h3>Cities:</h3>
<ul class="list">
    @foreach (var city in Model)
    {
        <li>@city</li>
    }
</ul>